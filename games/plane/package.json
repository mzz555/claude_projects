{
    "name": "@cp/game-plane",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "tsc -b && vite build",
        "typecheck": "tsc --noEmit"
    },
    "dependencies": {
        "phaser": "^3.80",
        "@cp/core": "workspace:*",
        "@cp/marble-sim": "workspace:*"
    },
    "devDependencies": {
        "vite": "^5",
        "typescript": "^5.5"
    }
}
